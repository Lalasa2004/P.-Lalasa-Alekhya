#include<stdio.h>
#include<conio.h>
int main()
{
int num;printf(\n enter the number:");
scanf("%d", &num);
int i, count=0;
printf("\n thr factors of %d are:");
for(i=1;i<=num;i++)
{
if(num%i==0)
{
++count;
printf("%d", i);
}
}
printf("\n\n total factors of %d:");
return 0;
}
